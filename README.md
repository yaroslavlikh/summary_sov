# Summary Bot - Telegram бот для суммаризации переписок

Telegram-бот, который автоматически сохраняет сообщения пользователей и создает краткие содержания переписок с помощью Google Gemini API.

## Возможности

- **Автоматическое сохранение сообщений** - все текстовые сообщения сохраняются в базу данных SQLite
- **Автоматическая суммаризация** - после каждых 150 сообщений автоматически создается краткое содержание
- **Ручная суммаризация** - команда `/summary` позволяет создать краткое содержание в любой момент
- **Настраиваемое количество сообщений** - можно указать количество последних сообщений для суммаризации
- **Использование Google Gemini API** - для создания качественных кратких содержаний

## Требования

- Python 3.7+
- Telegram Bot Token
- Google Gemini API Key

## Установка

1. Клонируйте репозиторий или скачайте проект

2. Установите зависимости:
```bash
pip install pyTelegramBotAPI google-genai python-dotenv
```

3. Создайте файл `.env` в корне проекта (можно скопировать из `example.env`):
```env
BOT_TOKEN=ваш_токен_telegram_бота
GEMINI_API_KEY=ваш_ключ_gemini_api
```

4. Получите токены:
   - **Telegram Bot Token**: Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
   - **Gemini API Key**: Получите ключ на [Google AI Studio](https://makersuite.google.com/app/apikey)

## Запуск

```bash
python main.py
```

После запуска вы увидите сообщение "Бот запущен", и бот начнет обрабатывать сообщения.

## 📖 Использование

### Команды бота

- `/help` - Показать справку по доступным командам
- `/summary [количество]` - Создать краткое содержание последних сообщений
  - Пример: `/summary 50` - создаст краткое содержание последних 50 сообщений
  - По умолчанию: 150 сообщений

### Как это работает

1. Бот автоматически сохраняет все текстовые сообщения (кроме команд) в базу данных
2. Сообщения от бота `sglypa_tg_bot` игнорируются
3. После каждых 150 сообщений автоматически создается краткое содержание
4. Вы можете вручную запросить краткое содержание в любой момент командой `/summary`

## Структура проекта

```
summary_sov/
├── main.py                 # Точка входа, запуск бота
├── config.py              # Конфигурация, загрузка переменных окружения
├── handlers/
│   └── handlers.py        # Обработчики сообщений и команд
├── llm/
│   ├── gemini.py          # Интеграция с Google Gemini API
│   ├── llama.py           # Альтернативная интеграция с Ollama (не используется)
│   └── prompt.py          # Промпт для LLM
├── database/
│   ├── init_db.py         # Инициализация базы данных
│   └── messages.sql       # SQLite база данных (создается автоматически)
├── example.env            # Пример файла с переменными окружения
└── README.md              # Этот файл
```

## База данных

Бот использует SQLite базу данных `database/messages.sql` со следующей структурой:

```sql
CREATE TABLE messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    user_name TEXT NOT NULL,
    message TEXT NOT NULL
);
```

База данных создается автоматически при первом запуске.

## ⚙️ Конфигурация

Все настройки хранятся в файле `.env`:

- `BOT_TOKEN` - токен Telegram бота
- `GEMINI_API_KEY` - ключ API для Google Gemini

## Модели LLM

Бот использует Google Gemini API:
- По умолчанию пытается использовать `gemini-3-flash-preview`
- При недоступности автоматически переключается на `gemini-1.5-flash`

Альтернативная поддержка Ollama (закомментирована в коде) может быть активирована при необходимости.

## Формат краткого содержания

Краткое содержание создается в формате списка с вложенными пунктами:
- Используется заголовок `#summary`
- Ключевые темы перечисляются с новой строки, начиная с дефиса
- Подпункты оформляются с отступом в 2 пробела
- Сохраняется оригинальный тон переписки (шутки, сарказм, мемы)

## Устранение неполадок

### Бот не отвечает
- Проверьте правильность `BOT_TOKEN` в файле `.env`
- Убедитесь, что бот запущен и работает

### Ошибка при суммаризации
- Проверьте правильность `GEMINI_API_KEY` в файле `.env`
- Убедитесь, что у вас есть доступ к Google Gemini API
- Проверьте баланс/лимиты API ключа

### База данных не создается
- Убедитесь, что у приложения есть права на запись в директорию `database/`
- Проверьте, что директория `database/` существует

## Лицензия

Этот проект создан в образовательных целях.

## Автор

Проект разработан для автоматизации суммаризации Telegram переписок.

---

**Примечание**: Бот автоматически добавляет напоминание от компании Google в конце каждого краткого содержания.
